export const RULES_CONFIG = {
  heroHealth: 20,
  deckSize: 20,
  normalCopyLimit: 2,
  handLimit: 10,
  boardLimit: 5,
  maxMana: 8,
  maxActionsPerChain: 100,
  maxTriggerDepth: 20,
  maxSummonsPerChain: 30,
} as const;
